import { kCreateWebError } from './kCreateWebError';
export function kAssert(condition, message, code = 'Package-Assertion') {
  if (!condition) {
    throw kCreateWebError({
      code,
      message
    });
  }
}
//# sourceMappingURL=kAssert.js.map